import React from "react";

import { HeaderContainer, HeaderTitle } from "./roles-header.styles";

const RolesHeader: React.FC = () => {
  return (
    <HeaderContainer>
      <HeaderTitle>نقش ها</HeaderTitle>
    </HeaderContainer>
  );
};

export default RolesHeader;
